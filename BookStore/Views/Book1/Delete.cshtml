@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_LayoutAngularjs.cshtml";
}
<h2>Delete</h2>
@Html.AntiForgeryToken()
<div ng-controller="DeleteCtrl">
    <table class="table">
        <tr>
            <td>確認刪除?</td>
        </tr>
        <tr>
            <td>ID</td>
            <td>{{Book.ID}}</td>
        </tr>
        <tr>
            <td>Author</td>
            <td>{{Book.Author}}</td>
        </tr>
        <tr>
            <td>Price</td>
            <td>{{Book.Price}}</td>
        </tr>
    </table>
    <input class="btn btn-primary" type="button" value="確定" ng-click="CallDeleteBook()" />
    <a ng-href="~/Book1/Index">  返回首頁</a>
</div>
@section Scripts {
    <script>
        var bookid = @ViewBag.ID;
    </script>
    <script src="~/Scripts/WebApp/Book1/DeleteCtrl.js"></script>
}